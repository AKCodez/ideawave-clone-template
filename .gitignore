# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
/build

# generated
/src/generated/
*.tsbuildinfo
next-env.d.ts

# env files - never commit a real one
.env*
!.env.example

# deployment
.vercel

# clone studio working directory (spec, report, scratch)
/.clone/

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
/coverage

# preview and audit output: regenerated by scripts/, and the sandbox clones this
# repo on every build, so screenshots must not ride along
/preview/shots/
/preview/og/
/preview/audit/
